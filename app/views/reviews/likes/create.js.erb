document.getElementById('like_count').innerText = '<%= @review.likes.count %> Likes';
document.getElementById('like-button').innerHTML = "<%= escape_javascript(link_to 'Unlike', movie_review_like_path(@review.movie, @review, @like, format: :js), method: :delete, remote: true) %>";